# Backend

Express API for the QualiTrack MVP.

## Scripts

- `npm run dev` starts the API with `node --watch`
- `npm start` starts the API normally

## Current Endpoints

- `GET /api/health`
- `GET /api/qc/incoming/summary`
- `POST /api/qc/incoming`
