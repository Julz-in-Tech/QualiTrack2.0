# Frontend

React client for the QualiTrack MVP.

## Scripts

- `npm run dev` builds the app and serves `dist/`
- `npm run build` creates a production build
- `npm run preview` serves the production build locally
